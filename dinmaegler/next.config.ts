import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com', 'dinmaegler.onrender.com','res.cloudinary.com' ], // Tilføjer placeholder-domenet
  },
};

export default nextConfig;
